let names = ["Neeraj", "neelam", "shivraj", "Dheeraj"];

let sortedNames = names.slice().sort();

console.log(
  "The sorted names are as follows(names staring with capital letter are sorted before by default",
  sortedNames
);
